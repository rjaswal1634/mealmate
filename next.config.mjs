/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com', 'img.spoonacular.com'],// Add this to allow GitHub avatars
    },
  };
  
  export default nextConfig;
  